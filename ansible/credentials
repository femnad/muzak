[default]
aws_access_key_id = {{ lookup('passwordstore', '{{ access_key_secret }} subkey=access-key-id') }}
aws_secret_access_key = {{ lookup('passwordstore', '{{ access_key_secret }} subkey=secret-access-key') }}
